spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/ankit5609/codeforge-config-server.git
      username: ankit5609
#      password: {GITHUB TOKEN}
      password: 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
      default-label: main

server.port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/ankit5609/codeforge-config-server.git
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false