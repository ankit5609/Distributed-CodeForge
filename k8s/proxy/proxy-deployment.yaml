# k8s/proxy/proxy-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: codeforge-me-proxy
  namespace: codeforge-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codeforge-me-proxy
  template:
    metadata:
      labels:
        app: codeforge-me-proxy
    spec:
      containers:
        - name: codeforge-me-proxy
          image: ankit5609/codeforge-me-proxy:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: codeforge-shared-config
          env:
            - name: REDIS_URL
              value: "redis://redis-0.redis.codeforge-core.svc.cluster.local:6379" 
            - name: PORT
              value: "80"
---
apiVersion: v1
kind: Service
metadata:
  name: codeforge-me-proxy
  namespace: codeforge-core
spec:
  selector:
    app: codeforge-me-proxy
  ports:
    - port: 80
      targetPort: 80