
# 1. Create a dedicated ServiceAccount in codeforge-core
apiVersion: v1
kind: ServiceAccount
metadata:
  name: workspace-service-account
  namespace: codeforge-core

---
# 2. Create a Role in codeforge-previews with the exact permissions Fabric8 needs
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: preview-manager-role
  namespace: codeforge-previews
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/exec", "pods/log", "pods/status"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

---
# 3. Bind the ServiceAccount (from codeforge-core) to the Role (in codeforge-previews)
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: preview-manager-binding
  namespace: codeforge-previews
subjects:
  - kind: ServiceAccount
    name: workspace-service-account
    namespace: codeforge-core # Note the cross-namespace reference here
roleRef:
  kind: Role
  name: preview-manager-role
  apiGroup: rbac.authorization.k8s.io

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: workspace-service
  namespace: codeforge-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workspace-service
  template:
    metadata:
      labels:
        app: workspace-service
    spec:
      serviceAccountName: workspace-service-account
      containers:
        - name: workspace-service
          # [BUG FIX - 2025-06]: Using exact image digest instead of tag.
          # GKE Autopilot sometimes uses a cached layer/image for snapshot tags even with
          # imagePullPolicy: Always if the tag has not explicitly changed (or registry sync lag).
          # Using the exact digest guarantees that Kubernetes pulls our new code.
          image: ankit5609/codeforge-workspace-service@sha256:2dfa54bcb1477cc75718a122c8e82b979d0700ec082acb52cc89b22966af3a0b
          imagePullPolicy: Always
          ports:
            - containerPort: 9020
          envFrom:
            - configMapRef:
                name: codeforge-shared-config
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: CONFIG_SERVER_URL
              value: http://config-service.codeforge-core.svc.cluster.local:8888
            - name: SPRING_CLOUD_CONFIG_FAIL_FAST
              value: "false"
            - name: SPRING_CLOUD_CONFIG_RETRY_MAX_ATTEMPTS
              value: "10"
            - name: SPRING_CLOUD_CONFIG_RETRY_INITIAL_INTERVAL
              value: "3000"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: WORKSPACE_DB_PASSWORD
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ROOT_USER
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ROOT_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /workspace/actuator/health
              port: 9020
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /workspace/actuator/health
              port: 9020
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: workspace-service
  namespace: codeforge-core
spec:
  selector:
    app: workspace-service
  ports:
    - port: 80
      targetPort: 9020